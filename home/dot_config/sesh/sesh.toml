#:schema https://github.com/joshmedeski/sesh/raw/main/sesh.schema.json

# Blacklist entries are regexes matched against session names.
# Hides worktree dirs (workmux + t3) and herdr workspaces from the picker.
blacklist = ["worktrees", "workspaces", "wm-"]
